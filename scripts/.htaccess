# Maintenance scripts (backup .bat, diagnostic PHP CLI tools).
# These are CLI-only — the web layer should never serve them.

# Apache 2.4+
Require all denied

# Apache 2.2 fallback
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
